{
  "name": "campus-guide-server",
  "version": "0.1.0",
  "private": true,
  "description": "A Node.js backend that directs the Campus Guide application to various configuration files.",
  "main": "src/server.js",
  "scripts": {
    "build": "yarn run clean && yarn run tsc && cp -r ./assets/ ./artifacts/assets/",
    "clean": "rimraf artifacts",
    "lint": "tslint src/**/*.ts",
    "start": "yarn run build && yarn run uglify && node ./artifacts/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "tscp": "tsc --pretty",
    "uglify": "./scripts/uglify"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/joseph-roque/campus-guide-server.git"
  },
  "author": "Joseph Roque",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/joseph-roque/campus-guide-server/issues"
  },
  "homepage": "https://github.com/joseph-roque/campus-guide-server#readme",
  "devDependencies": {
    "@types/express": "^4.0.36",
    "@types/node": "^8.0.7",
    "rimraf": "^2.6.1",
    "tslint": "^5.4.3",
    "tslint-eslint-rules": "^4.1.1",
    "typescript": "^2.4.1"
  },
  "dependencies": {
    "express": "^4.15.2",
    "http-status-codes": "^1.0.6",
    "jsonschema": "^1.1.0",
    "request": "^2.81.0",
    "request-promise-native": "^1.0.4",
    "uglify-js": "^3.0.23"
  }
}
