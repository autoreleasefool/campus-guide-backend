{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Shuttle Details",
  "description": "Additional details for travelling between campuses on the shuttle",
  "type": "array",
  "items": {
    "type": "object",
    "description": "A set of details pertaining to some use of the shuttle",
    "required": ["image", "icon"],
    "properties": {
      "image": {
        "type": "string",
        "description": "An image representing the details being provided."
      },
      "icon": { "$ref": "/icon" }
    },
    "patternProperties": {
      "^name(_[a-z]+)?$": {
        "type": "string",
        "description": "Name describing the details. Add the locale modifier for names in different languages"
      },
      "^details(_[a-z]+)?$": {
        "type": "array",
        "description": "List of details. Add the locale modifier for details in different languages",
        "items": {
          "type": "string",
          "description": "A paragraph in the details."
        }
      }
    }
  }
}
