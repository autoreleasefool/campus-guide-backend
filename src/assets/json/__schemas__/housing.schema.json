{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Housing",
  "description": "Sections within the app describing living in Ottawa and at the university",
  "type": "object",
  "additionalProperties": false,
  "required": [ "residences", "sections" ],
  "properties": {
    "residences": {
      "type": "array",
      "description": "List of residences at the university",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [ "shorthand", "image", "location", "properties" ],
        "properties": {
          "shorthand": {
            "type": "string",
            "description": "A shorthand code for the residence name"
          },
          "image": {
            "type": "string",
            "description": "An image of the residence"
          },
          "location": {
            "type": "object",
            "description": "Location of the residence",
            "additionalProperties": false,
            "required": [ "lat", "long" ],
            "properties": {
              "lat": {
                "type": "number",
                "description": "Latitude of the location"
              },
              "long": {
                "type": "number",
                "description": "Longitude of the location"
              }
            }
          },
          "properties": {
            "type": "object",
            "patternProperties": {
              ".+": {
                "description": "A property of the residence"
              }
            }
          }
        },
        "patternProperties": {
          "^name(_[a-z]+)?$": {
            "type": "string",
            "description": "Name of the residence. Add the locale modifier for names in different languages"
          },
          "^address(_[a-z]+)?$": {
            "type": "string",
            "description": "Address of the residence. Add the locale modifier for addresses in different languages"
          },
          "^description(_[a-z]+)?$": {
            "type": "string",
            "description": "Description of the residence. Add the locale modifier for descriptions in different languages"
          }
        }
      }
    },
    "sections": {
      "type": "array",
      "description": "Sections of housing information describing living at the university",
      "items": { "$ref": "/section" }
    },
    "resources": {
      "type": "array",
      "description": "List of other resources for finding housing at the university",
      "items": { "$ref": "/link" }
    }
  }
}
