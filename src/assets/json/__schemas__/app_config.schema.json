{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Configuration",
  "description": "Describes the global configuration for the application",
  "type": "object",
  "required": ["semesters", "university", "bus"],
  "additionalProperties": false,
  "properties": {
    "semesters": {
      "type": "array",
      "items": {
        "type": "object",
        "description": "List of semesters for which data and configurations are available",
        "required": ["id"],
        "additionalProperties": false,
        "properties": {
          "is_current": {
            "type": "boolean",
            "description": "Indicates if this is the current semester (true) or not (false/non-existant)"
          },
          "id": {
            "type": "string",
            "description": "Unique identifier for the semester"
          }
        },
        "patternProperties": {
          "^name(_[a-z]+)?$": {
            "type": "string",
            "description": "Name of the semester. Add the locale modifier for names in different languages"
          }
        }
      }
    },
    "university": {
      "type": "object",
      "description": "Provides properties detailing the university",
      "required": ["lat", "long"],
      "additionalProperties": false,
      "properties": {
        "lat": {
          "type": "number",
          "description": "Latitude of the school"
        },
        "long": {
          "type": "number",
          "description": "Longitude of the school"
        }
      },
      "patternProperties": {
        "^name(_[a-z]+)?$": {
          "type": "string",
          "description": "Name of the semester. Add the locale modifier for names in different languages"
        }
      }
    },
    "bus": {
      "type": "object",
      "description": "Details about the bus system which the city the school is in uses",
      "additionalProperties": false,
      "patternProperties": {
        "^name(_[a-z]+)?$": {
          "type": "string",
          "description": "Name of the bus company. Add the locale modifier for names in different languages"
        },
        "^link(_[a-z]+)?$": {
          "type": "string",
          "description": "Link to the website of the bus company. Add the locale modifier for websites in different languages"
        }
      }
    }
  }
}
