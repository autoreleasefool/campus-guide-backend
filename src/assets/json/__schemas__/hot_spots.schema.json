{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Hot Spots",
  "description": "A list of Yelp IDs for businesses",
  "type": "array",
  "items": {
    "type": "object",
    "description": "Details of a single Yelp business",
    "required": ["on_campus", "category", "business_id"],
    "additionalProperties": false,
    "properties": {
      "on_campus": {
        "type": "boolean",
        "description": "If true, then the destination is on the university's campus. Otherwise, the destination is near or within the city, generally close to the university"
      },
      "category": {
        "type": "string",
        "description": "Category of good or service that the destination provides",
        "enum": ["food", "group", "tour"]
      },
      "business_id": {
        "type": "string",
        "description": "ID from Yelp for the business"
      }
    }
  }
}
