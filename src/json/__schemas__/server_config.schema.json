{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Server Configuration",
  "description": "Describes the current versions and locations of configuration files for the application",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["name", "versions"],
    "additionalProperties": false,
    "properties": {
      "name": {
        "type": "string"
      },
      "versions": {
        "type": "object",
        "description": "Dictionary of versions of the application corresponding to most recent versions of the file",
        "additionProperties": false,
        "patternProperties": {
          "^([0-9]+\\.?)+$": {
            "type": "object",
            "required": ["version", "location"],
            "additionalProperties": false,
            "properties": {
              "version": {
                "type": "number",
                "description": "The current version of the file available at the specified location"
              },
              "disabled": {
                "type": "boolean",
                "description": "Indicates a certain file should not be downloaded by the application"
              },
              "location": {
                "type": "object",
                "description": "Location of the file, either locally or from a remote URL",
                "required": ["url"],
                "additionalProperties": false,
                "properties": {
                  "url": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}